<?php
    namespace myProject\backend\models;

    class User extends baseModel{
        protected $table = 'users';   
        protected $primaryKey = 'id';

        // Enter specific queries here (like find by a column that is not the primaryKey)
    }

?>